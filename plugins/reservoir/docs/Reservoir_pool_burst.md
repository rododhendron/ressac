+++
examples = []
name = "Reservoir.pool_burst"
short = "Route IV — pool N neurons into K bins, gain accumulates with spike count. kwargs: bins, frames_per_cycle, layout, drive, gain_per_spike, max_gain, burst_dur, mapping=:roundrobin|:hash."
tags = ["reservoir", "route", "pool"]
+++

# Reservoir.pool_burst

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
