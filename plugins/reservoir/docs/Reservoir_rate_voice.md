+++
examples = []
name = "Reservoir.rate_voice"
short = "Route V — measured spike rate (Hz) → oscillator freq. A neuron firing at 200 spikes/sec → 200 Hz tone. kwargs: sources (Vector{Int}, Vector{Vector{Int}}, or :all), shape=:impulse|:saw|:sin|:pulse, frames_per_cycle, freq_scale, freq_offset, lo_freq, hi_freq, gain, overlap, drive, smoothing_frames."
tags = ["reservoir", "route", "rate"]
+++

# Reservoir.rate_voice

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
