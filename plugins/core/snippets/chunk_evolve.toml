content_file = "chunk_evolve.jl"
context = "patterns"
description = "[Tidal idiom] chunk(4, fast(2)) — one quarter per cycle gets sped up."
includes = []
mode = "block"
name = "chunk_evolve"
requires_plugins = []
tags = ["tidal"]
