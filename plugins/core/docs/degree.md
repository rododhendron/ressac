+++
examples = ["cps!(0.5); :scale minor", "@d1 :wobble |> degree(p\"0 2 4 7\") |> gain(0.6)  # scale-aware (no #s in mini-notation)"]
name = "degree"
short = "degree(x) — like n(x) but interprets x as a scale-degree in the current :scale (use `:scale minor` first)."
tags = []
+++

# degree

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
