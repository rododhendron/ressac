+++
examples = ["@d1 p\"bd ~ sn ~\"            # 4 events per cycle", "@d1 p\"[bd bd] ~ sn ~\"       # nested = same time, two kicks", "@d1 p\"bd <hh sn cp> bd ~\"   # < > alternates each cycle"]
name = ""
short = "p\"bd hh sn\" — mini-notation literal. Each whitespace-separated token = 1 event in the cycle."
tags = []
+++

# 

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
