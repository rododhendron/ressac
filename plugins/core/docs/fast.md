+++
examples = ["@d1 p\"bd hh sn hh\" |> fast(2)        # twice as fast", "@d1 p\"bd hh\" |> every(4, fast(3))    # triplet every 4th cycle"]
name = "fast"
short = "fast(n, p) or `p |> fast(n)` — compress time ×n. fast(2) plays twice in a cycle."
tags = []
+++

# fast

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
