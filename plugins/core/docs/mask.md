+++
examples = ["@d1 mask(p\"bd*8\", p\"1 0 1 1 0 1 0 1\")  # gate the kicks"]
name = "mask"
short = "mask(p, q::Pattern{Bool}) — gate p by q. Where q is true, p plays."
tags = []
+++

# mask

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
