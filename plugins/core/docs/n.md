+++
examples = ["@d1 :bd |> n(p\"0 3 5 0\")           # bd:0 bd:3 bd:5 bd:0 (sample variants)", "@d1 :wobble |> n(p\"0 3 7 12\") |> gain(0.7)   # root → m3 → fifth → octave", "@d1 :bass |> n(p\"<0 -5 7 -3>\")     # alternate the offset across cycles"]
name = "n"
short = "Note offset (semitones) for synths, or sample-variant index for sample banks."
tags = []
+++

# n

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
