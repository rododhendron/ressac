+++
examples = ["cps!(0.5)        # 30 BPM at 4 beats/cycle, default", "cps!(0.75)       # ~45 BPM", ":bpm             # tap-tempo: 4 hits + Enter sets cps live"]
name = "cps"
short = "Cycles per second — Ressac's tempo unit. 0.5 = 1 cycle / 2s (30 BPM @ 4 beats/cycle), 0.8 = ~48 BPM, 0.3 = 18 BPM. cps!(x) sets it live, :cps x is the TUI form."
tags = []
+++

# cps

(Migrated from inline `_PARAM_DOCS`. Body intentionally empty for now.)
